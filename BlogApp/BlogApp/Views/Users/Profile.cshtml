@model User

<div class="row">
    <div class="col-md-2">
        <div class="bg-white p-2">
            <img src="/img/@Model.Image" class="img-fluid" alt="">
        </div>
        <hr>    
        <div class="bg-white p-2">
            <h5>@Model.Name</h5>
        </div>
    </div>
    <div class="col-md-10">
        <div class="bg-white p-4">
            <div id="posts">
                <h5 class="my-4">Yayınlanan Gönderiler</h5>
                <ul class="list-group">
                    @foreach(var post in @Model.Posts)
                    {
                        <li class="list-group-item"><i class="bi bi-record-fill"></i> <a href="/posts/details/@post.Url">@post.Title</a></li>
                    }
                </ul>
            </div>
            <div id="comments">
                <h5 class="my-4">Paylaşılan Yorumlar</h5>
                <ul class="list-group">
                    @foreach(var comment in @Model.Comments)
                    {
                        <li class="list-group-item"><i class="bi bi-chat-right-text-fill"></i> @comment.Text<a href="/posts/details/@comment.Post.Url"> [@comment.Post.Title]</a></li>
                    }
                </ul>
            </div>
        </div>
    </div>
</div>